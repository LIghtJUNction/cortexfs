# Rust build output
/target/

# Node test fixtures and local package installs
/node_modules/

# Local-only design notes
/docs/DESIGN.md

# Local FUSE integration-test mount tree.
# Keep the mountpoint itself in git, but never upload files materialized by the
# mounted filesystem.
/tests/mounts/cortexfs/**
!/tests/mounts/cortexfs/

# Local test/runtime scratch data
/.cortexfs/
/.bash_history
/.cache/
/.config/
/.env
/.env.*
!/.env.example
/tmp/
*.log
*.pid
.codegraph
/.tokensave/
