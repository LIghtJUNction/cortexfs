[package]
name = "cortexfs-channel-sdk"
description = "Rust SDK for process-isolated CortexFS channel adapters."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
readme = "README.md"
keywords = ["agent", "chat", "channel", "sdk"]
categories = ["network-programming", "api-bindings"]

[dependencies]
cortexfs-channels = { version = "0.1.19", path = "../cortexfs-channels" }
thiserror = "2.0.20"

[lints]
workspace = true

[dev-dependencies]
tempfile = "3.27.0"
