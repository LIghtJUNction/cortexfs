[package]
name = "cortexfs-channels"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
readme = "README.md"
description = "Runtime-neutral multi-channel messaging abstractions for agents."
keywords = ["agent", "chat", "channel", "im"]
categories = ["network-programming", "api-bindings"]

[dependencies]
futures-core = "0.3.34"
serde = { version = "1.0.229", features = ["derive"] }
serde_json = "1.0.151"
thiserror = "2.0.20"

[lints]
workspace = true
